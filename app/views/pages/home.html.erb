<% if signed_in? %>
  <table class="front" summary="For signed-in users">
	<tr>
		<td class="main">
			<h1 class="micropost">What's up?</h1>
			<%= render 'shared/micropost_form' %>
			<%= render 'shared/feed' %>
		</td>
		<td class="sidebar round">
			<%= render 'shared/user_info' %>
		</td>
	</tr>
  </table>
<% else %>

<h1>
	Sample app home page
</h1>
<p>
	This is the home page <a href="http://rickyuan.tumblr.com">rickyuan</a>
</p>
<%= link_to "Sigh up now!", signup_path, :class => "signup_button round" %>

<% end %>